# For accessing named collision flags through code.
class_name CollisionConstants

enum {
	# Physical objects which must collide with each other.
	WORLD = 1,
	# Used for detecting what and where entities are.
	ENTITY = 2,
}
